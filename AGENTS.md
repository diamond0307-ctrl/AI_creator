# AGENTS.md — LP/UI実装ルール

## レイアウト構成の基本方針(エディトリアルデザイン)

LP・広告クリエイティブを制作する際は、「AIっぽい単純な縦積みレイアウト」を避け、雑誌広告やハイエンドな広告クリエイティブのように、画像・人物・文字・カード・CTAが同一画面内で関係し合う構成を標準とする。

### 避けるべきパターン

- 「見出し→本文→画像→本文→カード」のような単純な縦積み
- text → image → text → image と機械的に繰り返すハンバーガー型レイアウト
- 各セクションを独立した箱として積み上げるだけの構成
- カードの均一な反復(同じ形・同じ余白のカードをただ並べる)
- 中央揃えを多用した、いわゆるSaaSテンプレート風デザイン
- 人物写真を不自然な形で切り抜く
- 重要な顔の上に文字を誤って被せる

### 目指すべきパターン

- 各セクションを独立した箱ではなく、1枚のグラフィック(1枚の絵)として構成する
- 画像の横に文字を置く
- 必要に応じて文字と画像を重ねる(顔など重要部分は避ける)
- 左右非対称なレイアウトを積極的に使う
- 要素同士のオーバーラップを活用する
- 要素がセクション境界からはみ出す配置を許容する
- スマートフォン表示でも、何でもかんでも1カラムの縦積みに戻し切らない。エディトリアルなレイアウトの緊張感をモバイルでも可能な限り維持する
- 雑誌広告・エディトリアルデザイン・高品質な広告クリエイティブを構成の基準にする

### 素材の扱い

- 高品質なモックアップが必要な場合、粗いCSSの箱(border-radiusを付けただけのdivなど)で済ませず、実際の画像素材を作成・加工する
- 参考画像がある場合は、雰囲気だけを真似るのではなく、完成した設計図として扱い、レイアウト・比率・重なりを忠実に再構築する(詳細は `.agents/skills/lp-reference-reconstruction/SKILL.md` を参照)
- 参考画像を分析し、その構成を再現するために必要なレイヤーや素材(切り抜き画像、背景、モックアップなど)を都度判断してWeb上で再構築する
- 毎回同じ数の素材や同じ工程を固定的に踏襲しない。セクションごとに最適な方法(HTML/CSSで組むか、画像素材を作るか)を判断する

### 検証

- レイアウトに関わる重要な変更を行った後は、必ずブラウザでスクリーンショットを撮り、参考画像がある場合はそれと比較して位置・サイズ・重なり・タイポグラフィを調整する

## 基本方針

このプロジェクトでは、PC表示だけでなくスマートフォン表示を最優先で確認すること。

デザイン変更・文言変更・CTA追加などを行う際は、指示がなくても以下のUI品質を必ず確認し、必要に応じて自動調整すること。

## CTAのルール

- CTA内の主要テキストは視覚的に中央配置する
- アイコンや矢印が右側にあっても、テキスト本体がボタン中央から不自然にずれないようにする
- 左側にバッジや残数表示がある場合も、主要コピーの視覚的中心を優先する
- `justify-content: space-between` だけに頼らず、必要に応じてGridやabsolute配置を使って中央を維持する
- テキスト、バッジ、矢印が互いに干渉しない十分な余白を確保する
- CTAの高さ、左右padding、文字サイズはモバイル画面で窮屈にならないよう調整する

## モバイルタイポグラフィ

- 375px、390px、430px幅を最低限想定して調整する
- 見出しが不自然な位置で改行されないよう確認する
- 日本語の文字間隔を広げすぎない
- 本文は `letter-spacing` を原則 `0`〜`0.04em` 程度に抑える
- 大見出しも、日本語では英字ほど強いletter-spacingを付けない
- ボタン内テキストは読みやすさを優先し、文字間隔を過度に広げない
- 1文字ずつ間が空いて見える状態を避ける
- 行間は詰めすぎず、モバイルでは可読性を優先する

## レスポンシブ

変更後は最低限、以下の幅でレイアウト崩れがないか確認すること。

- 375px
- 390px
- 430px
- 768px
- 1280px

特に以下を確認する。

- 横スクロールが発生していない
- CTAが画面外にはみ出していない
- 見出しと本文が重なっていない
- 画像上の文字が顔や重要部分に被りすぎていない
- 固定ヘッダーとコンテンツが重ならない
- 固定CTAとページ下部CTAが干渉しない
- ボタン内テキストが2行になった場合でもレイアウトが破綻しない

## 日本語テキストの扱い

日本語は英語と同じ文字組みを適用しない。

特に以下を避ける。

- 過剰な `letter-spacing`
- 不自然な中央揃え
- 単語途中の不自然な改行
- ボタン幅いっぱいに文字を広げる
- 行間が狭すぎる見出し

必要に応じて `clamp()`、`max-width`、`line-height`、メディアクエリを使用して自然に調整する。

## デザイン変更時の判断

ユーザーから明示的な指示がなくても、表示上の問題を見つけた場合は以下を自動で修正すること。

- CTAの文字位置
- モバイル時の文字サイズ
- 文字間隔
- 改行位置
- セクション余白
- 要素の重なり
- 横幅オーバー
- 固定要素との干渉

ただし、ブランドカラー、主要コピー、写真、世界観を大きく変更する場合は勝手に変更しない。

## 最終確認

変更完了前に、必ずモバイル表示を確認する。

特にCTAについて、

「文字が本当に中央に見えるか」

「一目で押せるボタンに見えるか」

「日本語の文字間隔が自然か」

を視覚的に確認してから完了とする。
